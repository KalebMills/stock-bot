import sys, json
from matplotlib import pyplot as plt

# TRADE_EVENTS_PATH = sys.argv[1] #Trade Events

# BOT_ACTIONS_PATH = sys.argv[2] #A file split line by line of timestamps indicating where it would purchase at

# TRADE_EVENTS = None
# BOT_ACTIONS = None

# with open(TRADE_EVENTS_PATH) as T:
#     TRADE_EVENTS = json.load(T)

# with open(BOT_ACTIONS) as B:
#     BOT_ACTIONS = json.load(B)

plt.plot([1, 2, 3, 4, 5])

plt.savefig('./test')